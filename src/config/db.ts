// PostgreSQL Database Configuration
export const dbConfig = {
    host: '127.0.0.1',
    port: 5432,
    database: 'readcircle',
    schema: 'readcircle',
    user: 'postgres', // Default PostgreSQL user, replace if different
    password: 'renato', // Add your password here
    ssl: false // Set to true if SSL is required
};
